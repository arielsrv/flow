name: 🚀 Release

# ─────────────────────────────────────────────────────────────────────────────
# TRIGGER
#   • tag push v*.*.*  → build + push to ACR
#
# Once the image is published, deploy it using the "Deploy Manual" workflow
# by selecting the target environment.
# ─────────────────────────────────────────────────────────────────────────────
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

env:
  ACR_NAME: myregistry
  IMAGE_NAME: my-app

jobs:

  build-and-push:
    name: 🐳 Build & Push → ACR
    runs-on: ubuntu-latest

    outputs:
      image: ${{ steps.meta.outputs.image }}
      tag:   ${{ steps.meta.outputs.tag }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔖 Extract tag metadata
        id: meta
        run: |
          TAG="${GITHUB_REF_NAME}"
          IMAGE="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}"
          echo "tag=${TAG}"     >> "$GITHUB_OUTPUT"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"
          echo ""
          echo "📌 Detected tag  : ${TAG}"
          echo "🖼️  Target image  : ${IMAGE}:${TAG}"

      - name: 📦 Production build (simulated)
        run: |
          echo ""
          echo "🔨 Production build..."
          echo "  → [1/4] Resolving dependencies .............. ✅"
          sleep 1
          echo "  → [2/4] Compiling in RELEASE mode ........... ✅"
          sleep 1
          echo "  → [3/4] Running pre-package tests ........... ✅"
          sleep 1
          echo "  → [4/4] Generating final artifact ........... ✅"
          echo ""
          echo "✅ Production build ready"

      - name: 🔐 ACR login (simulated)
        run: |
          echo ""
          echo "🔐 Authenticating to Azure Container Registry..."
          echo "  → az acr login --name ${{ env.ACR_NAME }}"
          echo "✅ Logged in to ${{ env.ACR_NAME }}.azurecr.io"

      - name: 🐳 Docker build (simulated)
        run: |
          IMAGE="${{ steps.meta.outputs.image }}"
          TAG="${{ steps.meta.outputs.tag }}"
          echo ""
          echo "🐳 Building Docker image..."
          echo "  → docker build \\"
          echo "       --platform linux/amd64 \\"
          echo "       --label org.opencontainers.image.revision=${{ github.sha }} \\"
          echo "       --label org.opencontainers.image.created=$(date -u +%Y-%m-%dT%H:%M:%SZ) \\"
          echo "       -t ${IMAGE}:${TAG} \\"
          echo "       -t ${IMAGE}:latest \\"
          echo "       ."
          sleep 2
          echo ""
          echo "✅ Image built:"
          echo "   • ${IMAGE}:${TAG}"
          echo "   • ${IMAGE}:latest"

      - name: 📤 Docker push to ACR (simulated)
        run: |
          IMAGE="${{ steps.meta.outputs.image }}"
          TAG="${{ steps.meta.outputs.tag }}"
          echo ""
          echo "📤 Pushing image to ACR..."
          echo "  → docker push ${IMAGE}:${TAG}"
          sleep 1
          echo "     Pushed: sha256:a1b2c3d4e5f6..."
          echo "  → docker push ${IMAGE}:latest"
          sleep 1
          echo "     Pushed: sha256:a1b2c3d4e5f6..."
          echo ""
          echo "✅ Image published successfully"

      - name: 📋 Build summary
        run: |
          IMAGE="${{ steps.meta.outputs.image }}"
          TAG="${{ steps.meta.outputs.tag }}"
          echo "## 🐳 Image published to ACR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Registry** | \`${{ env.ACR_NAME }}.azurecr.io\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Image** | \`${IMAGE}:${TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Tag** | \`${TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Published by** | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> ✅ Image ready. To deploy, run the **Deploy Manual** workflow and select the target environment." >> $GITHUB_STEP_SUMMARY
